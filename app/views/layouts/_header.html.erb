<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-menu">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="localhost:3000">ORE-NO-BLOG</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-menu">
      <ul class="nav navbar-nav navbar-right">
      <% if author_signed_in? %>
       <p class="navbar-text navbar-right"><%= current_author.name %>さんでログイン中</p>
     <% end %>
      <% if author_signed_in? %>
        <li><%= link_to '新規投稿を作成', new_post_path %></li>
        <li><%= link_to '自身の情報を編集', edit_author_registration_path %></li>
        <li><%= link_to 'ログアウト' , destroy_author_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to '新規登録', new_author_registration_path %></li>
        <li><%= link_to 'ログイン', new_author_session_path %></li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
